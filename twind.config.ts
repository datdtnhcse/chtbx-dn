import { Configuration } from "https://esm.sh/v99/twind@0.16.18/twind.d.ts";

const config: Configuration = {
	mode: "strict",
	theme: {
		fontFamily: {
			display: ["Big Shoulders Stencil Display", "sans-serif"],
			body: ["Jura", "sans-serif"],
		},
	},
};

export default config;
